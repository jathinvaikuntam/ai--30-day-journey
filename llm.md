An llm is a large language model
It’s a type of AI trained on huge amounts of text.
It learns patterns in language and can generate human-like text, answer questions, summarize, translate, and more.

Token 

when the input is broken into a bunch of small pieces each piece is assigned a high - dimensional vector is known as token


Context window 

It is the mechanism in which the network (or) the ai model which can only convert a fixed number of tokens at a time is known as context window 


Temparature is low there would be only a single prediction . Temparature is high then there would be more probability of of many variety and randomness


System prompt
A system prompt is an instruction given to an LLM that sets the overall behavior or role before the user messages start.